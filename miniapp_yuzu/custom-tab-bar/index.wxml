<view wx:if="{{visible}}" class="custom-tabbar {{roleClass}}">
  <view
    wx:for="{{list}}"
    wx:key="pagePath"
    data-index="{{index}}"
    bindtap="switchTab"
    class="tab-item {{selected === index ? 'active' : ''}}"
  >
    <view class="tab-label">{{item.text}}</view>
  </view>
</view>
